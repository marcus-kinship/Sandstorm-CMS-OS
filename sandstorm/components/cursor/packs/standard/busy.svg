<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
    <!-- Spinner ring — center-anchored. `.cursor-busy-spin` (renderer.js, when the
         "Busy animation" toggle is on) spins this around its own center. -->
    <path fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round"
        d="M12 4 a8 8 0 1 1 -8 8" />
</svg>
